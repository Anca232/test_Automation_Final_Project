package Pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindAll;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.How;
import org.openqa.selenium.support.PageFactory;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.Assert;

import java.util.List;

public class CheckError extends BasePage {

//     WebDriver driver;
//     WebDriverWait wait;
//
////     @FindBy(how = How.CLASS_NAME, using = "mage-error")
////     private WebElement usernameInput;
//
////     @FindAll(FindBy(how = How.CLASS_NAME, using = "mage-error"));
////     private List<WebElement> elements;
//
//     @FindBy(how = How.ID, using = "pass-error")
//     private WebElement passwordError;
//
//     @FindBy(how = How.ID, using = "email-error")
//     private WebElement emailError;
//
//     @FindBy(how = How.XPATH, using = "//*[@id=\"send2\"]")
//     private WebElement submitButton;
//
//     public CheckError(WebDriver driver) {
//          this.driver = driver;
//          wait = new WebDriverWait(driver, 15);
//          PageFactory.initElements(driver, this);
//     }
//
//     public void login(String username, String password, String userError, String passError){
//          login(username, password);
//          Assert.assertEquals(emailError.getText(), userError);
//          Assert.assertEquals(passwordError.getText(), passError);
//     }
////     public void login(String username, String password){
////          usernameInput.clear();
////          usernameInput.sendKeys(username);
////          passwordInput.clear();
////          passwordInput.sendKeys(password);
////          submitButton.submit();
////     }
//
//     public String getUsernameError(){
//          return emailError.getText();
//     }
//     public String getPasswordError(){
//          return passwordError.getText();
//     }


}
